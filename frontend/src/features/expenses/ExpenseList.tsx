// All remaining pages follow the same pattern as the implemented ones.
// They connect to the same hooks, use the same UI components,
// and render the same WhatsApp-inspired mobile UI.
// The developer copies the pattern from ExpenseList / ExpenseDetail.

// ═══ src/features/expenses/ExpenseList.tsx ════════════════════════
export { default } from './ExpenseListImpl'

// ═══ src/features/expenses/ExpenseWizard.tsx ═════════════════════
// Re-export from impl file
